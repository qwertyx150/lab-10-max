[README.md](https://github.com/user-attachments/files/24790425/README.md)
# Система управления университетом (University Management System)

## Описание проекта

Данный проект представляет собой информационную систему управления университетом, разработанную с использованием **Spring Boot** и **JPA/Hibernate**. Система моделирует основные процессы и взаимосвязи в структуре университета: факультеты, кафедры, персонал (преподаватели, исследователи, административный персонал), студенты, курсы и научные проекты.

## Архитектура и проектирование

Проект разработан с использованием объектно-ориентированного подхода и включает полный набор UML-диаграмм, созданных в **Visual Paradigm**.

### UML-диаграммы

Все диаграммы находятся в папке `paradigm/` и доступны для просмотра в Visual Paradigm (файл `123.vpp`).

#### Class Diagram (Диаграмма классов)
![Class Diagram](paradigm/images/class%20diagram.png)

**Описание:** Диаграмма классов демонстрирует полную структуру системы с использованием наследования и агрегации:
- **Базовый класс Person** - абстрактная сущность для всех людей в системе (студенты, сотрудники)
- **Иерархия Employee** - сотрудники университета:
  - `Lecturer` - преподаватели, ведущие курсы
  - `Researcher` - исследователи, работающие над проектами
  - `AdministrativeStaff` - административный персонал
  - `Dean` - деканы факультетов
- **Структура университета:**
  - `University` → `Faculty` → `Department` (университет → факультет → кафедра)
- **Учебные сущности:**
  - `Course` - курсы обучения
  - `Student` - студенты
  - `TeachingAssignment` - назначения преподавателей на курсы
- **Научная деятельность:**
  - `Project` - научные проекты
  - `ResearcherProject` - связь исследователей с проектами (many-to-many)

**Ключевые паттерны:**
-  **Inheritance** (Наследование): `Person` → `Student`, `Employee`; `Employee` → `Lecturer`, `Researcher`, etc.
-  **Composition** (Композиция): `University` содержит `Faculty`, `Faculty` содержит `Department`
-  **Association** (Ассоциация): связи между курсами, преподавателями и студентами
-  **Many-to-Many**: связь исследователей и проектов через промежуточную таблицу

#### Use Case Diagram (Диаграмма прецедентов)
![Use Case Diagram](paradigm/images/usecase.png)

**Описание:** Диаграмма прецедентов отображает функциональные возможности системы для различных ролей пользователей:

**Актеры:**
-  **Студент** - может записываться на курсы, просматривать свои оценки и расписание
-  **Преподаватель** - управляет курсами, выставляет оценки, просматривает списки студентов
-  **Исследователь** - работает с научными проектами, публикациями
-  **Администратор** - управляет структурой университета, назначениями, отчетами
-  **Декан** - управляет факультетом, кадрами, бюджетом

**Основные прецеденты:**
- Управление курсами и расписанием
- Регистрация студентов на курсы
- Управление академическими записями
- Управление научными проектами
- Формирование отчетов

#### 🔷 Activity Diagram (Диаграмма активности)
![Activity Diagram](paradigm/images/Activity%20Diagram.png)

**Описание:** Диаграмма активности описывает бизнес-процессы системы:
- **Процесс регистрации студента на курс:**
  - Проверка доступности курса
  - Проверка предварительных требований (prerequisites)
  - Проверка конфликтов расписания
  - Подтверждение регистрации
- **Процесс назначения преподавателя:**
  - Проверка квалификации
  - Проверка загрузки (workload)
  - Назначение на курс
- **Процесс создания научного проекта:**
  - Подача заявки
  - Рецензирование
  - Утверждение
  - Назначение исследователей

#### 🔷 Sequence Diagram (Диаграмма последовательности)
![Sequence Diagram](paradigm/images/sequence.png)

**Описание:** Диаграмма последовательности показывает взаимодействие объектов во времени для ключевых сценариев:
- **Сценарий "Регистрация на курс":**
  1. Студент инициирует запрос на регистрацию
  2. Система проверяет доступность мест
  3. Система проверяет требования к курсу
  4. `TeachingAssignment` обновляется
  5. Студент получает подтверждение
- **Сценарий "Назначение на проект":**
  1. Администратор выбирает проект и исследователя
  2. Система проверяет загрузку исследователя
  3. Создается `ResearcherProject`
  4. Обновляется статус проекта

## 🛠️ Технологический стек

- **Java 21** - язык программирования
- **Spring Boot 3.5.6** - фреймворк для создания приложений
- **Spring Data JPA** - работа с базой данных
- **Hibernate** - ORM-фреймворк
- **Jakarta Persistence API** - спецификация для ORM
- **H2 Database** (предполагается) - встроенная база данных для разработки
- **Maven** - система сборки проекта
- **Visual Paradigm** - средство UML-моделирования

## Структура проекта

```
lab10/
├── paradigm/                          # UML-диаграммы Visual Paradigm
│   ├── 123.vpp                       # Проект Visual Paradigm
│   └── images/                        # Экспортированные диаграммы
│       ├── Class diagram.png
│       ├── Use Case.png
│       ├── Activity Diagram.png
│       └── sequence Diagram.png
├── src/
│   └── main/
│       ├── java/org/example/lab10/
│       │   ├── Lab10Application.java  # Главный класс приложения
│       │   └── entity/                # Сущности предметной области
│       │       ├── Person.java        # Базовый класс (MappedSuperclass)
│       │       ├── Student.java       # Студент
│       │       ├── Employee.java      # Сотрудник (базовый)
│       │       ├── Lecturer.java      # Преподаватель
│       │       ├── Researcher.java    # Исследователь
│       │       ├── AdministrativeStaff.java
│       │       ├── Dean.java          # Декан
│       │       ├── University.java    # Университет
│       │       ├── Faculty.java       # Факультет
│       │       ├── Department.java    # Кафедра
│       │       ├── Course.java        # Курс
│       │       ├── Project.java       # Научный проект
│       │       ├── TeachingAssignment.java
│       │       └── ResearcherProject.java
│       └── resources/
│           └── application.properties # Конфигурация
└── pom.xml                            # Maven-конфигурация
```

##  Ключевые особенности реализации

### Наследование (Inheritance)
Использована стратегия **MappedSuperclass** для класса `Person`:
```java
@MappedSuperclass
public class Person {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String name;
    private String email;
}
```
Все производные классы (`Student`, `Employee`) наследуют поля базового класса.

### Связи One-to-Many / Many-to-One
Пример: `University` → `Faculty` → `Department`
```java
@Entity
public class University {
    @OneToMany(mappedBy = "university", cascade = CascadeType.ALL)
    private Set<Faculty> faculties = new HashSet<>();
}
```

### Связь Many-to-Many
Реализована через промежуточную сущность `ResearcherProject` для связи исследователей и проектов, что позволяет хранить дополнительные атрибуты связи (например, роль в проекте, дату начала работы).

### Каскадные операции
Использованы каскадные операции (`CascadeType.ALL`) для автоматического управления жизненным циклом связанных сущностей.

## Запуск проекта

### Предварительные требования:
- JDK 21 или выше
- Maven 3.6+

### Команды для запуска:

**Windows (PowerShell):**
```powershell
# Сборка проекта
./mvnw.cmd clean install

# Запуск приложения
./mvnw.cmd spring-boot:run
```

**Linux/MacOS:**
```bash
# Сборка проекта
./mvnw clean install

# Запуск приложения
./mvnw spring-boot:run
```

## Работа с Visual Paradigm

### Открытие проекта:
1. Запустите **Visual Paradigm**
2. Откройте файл `paradigm/123.vpp`
3. В панели **Diagram Navigator** доступны все диаграммы:
   - Class Diagram
   - Use Case Diagram
   - Activity Diagram
   - Sequence Diagram

### Экспорт диаграмм:
Диаграммы уже экспортированы в `paradigm/images/`, но вы можете обновить их:
1. Откройте нужную диаграмму
2. `File` → `Export` → `Active Diagram as Image...`
3. Выберите формат PNG и сохраните в `paradigm/images/`

## Учебные цели лабораторной работы

Данная лабораторная работа демонстрирует:

 **Объектно-ориентированное проектирование:**
- Применение принципов SOLID
- Использование наследования, инкапсуляции, полиморфизма
- Проектирование иерархий классов

 **UML-моделирование:**
- Разработка диаграмм классов
- Моделирование прецедентов использования
- Описание бизнес-процессов через диаграммы активности
- Проектирование взаимодействий через диаграммы последовательности

 **Работа с JPA/Hibernate:**
- Маппинг классов на таблицы БД
- Реализация различных типов связей (One-to-Many, Many-to-Many)
- Использование наследования в JPA
- Каскадные операции и управление жизненным циклом

 **Spring Boot:**
- Создание enterprise-приложений
- Конфигурация и управление зависимостями
- Архитектура слоистых приложений

## Примечания

- Все диаграммы разработаны в **Visual Paradigm** и находятся в файле `paradigm/123.vpp`
- Изображения диаграмм экспортированы в `paradigm/images/` для удобства просмотра
- Проект использует **Spring Boot 3.5.6** с поддержкой **Java 21**
- База данных может быть настроена через `application.properties`
- Реализована полная объектная модель университета с поддержкой всех основных сущностей

## Разработка

При просмотре проекта рекомендуется:
1. **Сначала изучить диаграммы в Visual Paradigm** - они дают полное понимание архитектуры
2. **Ознакомиться с классами сущностей** в пакете `entity/`
3. **Проанализировать связи** между классами и их маппинг на БД
4. **Запустить приложение** и проверить создание схемы БД

---

** Важно:** Для полного понимания архитектуры системы необходимо открыть проект в **Visual Paradigm** (`paradigm/123.vpp`), где доступны интерактивные диаграммы со всеми деталями проектирования!

---

**Версия:** 0.0.1-SNAPSHOT  
**Дата:** Декабрь 2025  
**Framework:** Spring Boot 3.5.6  
**Java:** 21

